NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Optional — fine-grained PAT with read:user scope.
# Without this the contribution heatmap is hidden; events list still works.
# Enable "Include private contributions on my profile" in GitHub settings
# if you want private/org contribs counted.
GITHUB_TOKEN=
